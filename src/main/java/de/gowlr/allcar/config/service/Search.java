package de.gowlr.allcar.config.service;

/*
import java.util.List;

import org.springframework.stereotype.Service;
import org.hibernate.search.annotations.Field;
import org.hibernate.search.annotations.Indexed;

import java.util.Objects;


@Service
public class Search { 

    public Page<EcProductTypeEntity> getByQuery(String brandTitle, String model, String variant) {
    return repo.getByQuery(brandTitle, model, variant).map(mapper::toEcProductTypeEntity);
    }

}

*/